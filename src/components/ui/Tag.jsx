/**
 * Tag
 * Small rounded pill used to display tech stack labels on project cards
 * and experience items.
 */
export default function Tag({ children }) {
  return <span className="tag">{children}</span>;
}
